@model IEnumerable<BitBooking.DAL.Models.Token>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Token
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Used)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccomodationId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Hash)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Used)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccomodationId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TokenId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TokenId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TokenId })
        </td>
    </tr>
}

</table>
